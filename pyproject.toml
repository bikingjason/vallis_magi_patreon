[project]
name = "vallis_magi_patreon"
version = "0.1.0"
description = "Vallis Magi Patreon"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "tomli-w>=1.2.0",
    "windows-curses>=2.4.2; sys_platform == 'win32'",
]

[tool.uv.workspace]
members = ["vallis_magi_classic"]
