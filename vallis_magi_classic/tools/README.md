Common tools used in VM Classic that may be useable in other applications later.

## localisation.py

Given a locale loads the localised strings for that locale.
A locale can have a parent locale, which provides strings common to similar locales, like American and English.
