# This file only contains translation strings for Australian, currently none and I cannot think of any right now.
# The en.toml file is loaded first, then these entries overwrite the English versions.

[config]
parent = "en"
